# R48 — إغلاق الحفاظ على اللوجستيات وربط الدفعات

هذا الإصدار لا يغيّر حدود الترحيل المحاسبي في R46/R47 ولا يضيف Migration جديدة. هدفه تثبيت خصائص العمل الحالية ومنع تراجعها مستقبلًا.

## العقود المثبتة

- **الشراء:** تصديق مستند الاستلام هو الذي يزيد الكمية المخزنية ويسجل حركة `purchase_in`. أمر الشراء نفسه لا يغيّر الكمية ولا يملك القيد المحاسبي النهائي.
- **البيع:** تصديق مستند التسليم هو الذي ينقص الكمية المخزنية ويسجل حركة `sale_out`. أمر البيع نفسه لا يغيّر الكمية ولا يملك القيد المحاسبي النهائي.
- **الصيانة:** تصديق صرف المواد (`stock_issue_draft -> stock_issue_approved`) هو الذي ينقص كميات قطع الصيانة ويسجل `maintenance_out`. قيد الصيانة المالي يبقى عند اعتماد الفاتورة.
- **المحاسبة التجارية:** R46 محفوظة؛ الترحيل المحاسبي النهائي للشراء/البيع/الصيانة يبقى مملوكًا للفوترة.
- **الدفعات متعددة العملات:** عند اختلاف عملة الدفع عن عملة الفاتورة يجب اختيار صندوق مصدر بعملة الدفع وصندوق مرتبط بعملة الفاتورة. الخادم يتحقق من الربط في الاتجاهين ومن تطابق العملات قبل تنفيذ التسوية والتحويل.
- **الصيانة:** دفعات الصيانة تمر عبر نفس محرك `erp_execute_secure_linked_payment_v1` المستخدم للربط الآمن.
- **تحويل الصناديق:** مسار R47 القديم المحول إلى R22 لا يزال يرفض التحويل متعدد العملات بين صندوقين غير مرتبطين.

## التحقق

```powershell
npm run verify:r48
npm run verify:r47
npm run verify:r46
npm run verify:r44
npm run verify:database
npm run verify:structure
npm run verify:source
flutter analyze
flutter test
```

لا توجد Migration خاصة بـ R48؛ migrations المطلوبة للإنتاج تبقى R46 ثم R47 فقط إذا لم تكونا مطبقتين بعد.
