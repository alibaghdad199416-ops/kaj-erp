R15 FINAL SOURCE VERIFICATION
Sat Aug  8 00:21:25 UTC 2026

PASS R15 canonical-state reconciliation
  - deleted/purged tombstones permanently outrank stale master payloads unless explicitly restored
  - optimistic versions prevent older screens/devices from silently overwriting newer Supabase state
  - stale accounting sync cannot overwrite newer account/partner/item-cost state
  - obsolete capitalization accounts cannot reactivate; contaminated purchase invoices normalize from source
  - deleted/stale cashbox edits cannot revive or overwrite current definitions
  - cashbox opening balance and cash-side journals follow the current cashbox ledger binding continuously
  - production readiness exposes canonical data health and reconciliation drift
  - Supabase/Firebase production configuration hashes are unchanged

PASS PostgreSQL CREATE OR REPLACE compatibility — 1129 definitions checked, 623 active signatures
PASS PostgreSQL text/UUID type-boundary verification
  - 622 active function signatures inspected
  - 152 table schemas inspected
  - legacy text columns cannot be compared directly with UUID parameters or variables
  - JSON text extraction cannot be compared directly with UUID parameters or variables
  - erp_records company matching uses one explicit UUID-to-legacy-key boundary

PASS Supabase-only structural verification
  - Dart graph: 329 reachable source files; 3 explicitly test-only support files; no import cycles
  - RPC contract: 159 literal calls, all defined
  - Executable tests: 31; support files: 1
  - Migrations: 211

PASS static Dart source sanity verification
  - checked 332 Dart files
  - all quality_line_erp package imports and exports resolve
  - FontWeight values are supported by Flutter
  - no unresolved merge markers
PASS localization verification
  - English remains the primary locale and Arabic remains supported
  - AppText follows the active widget-tree locale
  - visible form properties and validators use localization

PASS R14 runtime RPC/invoice root closure
  - R14 master CRUD/tombstone contracts replace the failing R9 browser endpoint and validate legacy table structure
  - workflow status/accounting writes no longer require unrelated *.update field permission
  - Phase-26 browser contract is recreated behind an explicit PostgREST schema reload
  - sales/purchase invoice approval uses one diagnosable R14 contract over V23.0.2/V7.6.2
  - sales cross-definition currency and purchase single-currency accounting policies are retained
  - Supabase/Firebase production configuration hashes are unchanged


> verify:package
> python -B tool/verify_package_sanity.py

PASS clean package sanity verification
  - pubspec version: 22.9.8+229008
  - generated/local paths are excluded by .gitignore
  - Supabase is the application backend; Firebase is Hosting only
  - local credentials and generated folders are excluded
  - root command surface is concise and explicit

> verify:deployment-target
> python -B tool/verify_deployment_target.py

PASS production deployment target verification
  - runtime config: dart_defines.json
  - Supabase project: fjiaxdorunedmltgqtty
  - Firebase project: kaj-erp
  - public self-signup is disabled; ERP administrators create users inside the app
  - only a publishable browser key is packaged
