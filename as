CREATE OR REPLACE FUNCTION public.erp_list_cloud_sales_workflow_orders(p_company_id uuid) RETURNS setof jsonb LANGUAGE SQL SECURITY DEFINER SET search_path=public AS  SELECT jsonb_build_object( 'id', o.id::text, 'orderNumber', o.order_number, 'customerId', o.customer_id, 'customerName', coalesc(c.data_, ''), 'status', o.status, 'currency', o.currency, 'exchangeRate', o.exchange_rate, 'subtotal', o.subtotal, 'discount', o.discount, 'total', o.total, 'notes', o.notes, 'createdAt', o.created_at::text, 'updatedAt', o.updated_at::text, 'deliveryId', d.id::text, 'deliveryStatus', d.status, 'invoiceId', i.id::text, 'invoiceStatus', i.status, 'invoiceRemaining', coalesce(nullif(i.payload_,'' )::numeric, 0), 'createdBy', o.created_by::text, 'createdByName', coalesce(nullif(btrim(p.full_name), ''), o.created_by::text, ''), 'carId', r.carId, 'carName', nullif( btrim( concat_ws( ' ', car.data_, car.data_, car.data_) ), '') ) FROM erp_sales_orders_cloud o LEFT JOIN erp_customers c ON c.id = o.customer_id AND c.company_id = o.company_id AND NOT c.is_deleted LEFT JOIN lateral ( SELECT x.* FROM erp_commercial_workflow_documents x WHERE x.company_id = o.company_id AND x.module = 'sales' AND x.document_type = 'delivery' AND x.parent_id = o.id AND NOT x.is_deleted ORDER BY x.created_at DESC LIMIT 1 ) d ON TRUE LEFT JOIN lateral ( SELECT x.* FROM erp_commercial_workflow_documents x WHERE x.company_id = o.company_id AND x.module = 'sales' AND x.document_type = 'invoice' AND x.parent_id = o.id AND NOT x.is_deleted ORDER BY x.created_at DESC LIMIT 1 ) i ON TRUE LEFT JOIN public.profiles p ON p.id = o.created_by LEFT JOIN LATERAL ( SELECT payload- carId FROM erp_records WHERE company_id = o.company_id AND entity_type = 'opportunities' AND record_id = o.opportunity_id LIMIT 1 ) r ON TRUE LEFT JOIN erp_cars car ON car.id = nullif(btrim(r.carId), '') AND car.company_id = o.company_id WHERE o.company_id = p_company_id AND NOT o.is_deleted AND public.erp_is_company_member(p_company_id) ORDER BY o.created_at DESC;  REVOKE ALL ON FUNCTION public.erp_list_cloud_sales_workflow_orders(uuid) FROM PUBLIC, ANON; GRANT EXECUTE ON FUNCTION public.erp_list_cloud_sales_workflow_orders(uuid) TO authenticated
